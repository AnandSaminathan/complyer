MODULE main

VAR a : boolean;
VAR b : boolean;
VAR c : boolean;

ASSIGN

init(a) := true;
init(b) := false;
init(c) := false;

next(a) := c;
next(b) := a;
next(c) := b;

SAFETYSPEC
((a && !b && !c) || (!a && b && !c) || (!a && !b && c))
